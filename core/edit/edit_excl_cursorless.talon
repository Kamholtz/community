not tag: user.cursorless
-
# These were originally in edit.talon, but the `clone line` command interfered with the 'clone' Cursorless on Linux. The other command was also moved in here because it came from the same section.

# Duplication
clone that: edit.selection_clone()
clone line: edit.line_clone()